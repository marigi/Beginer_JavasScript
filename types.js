const name = 'benji';
const middle = 'marigi';
const last = 'ndegwa';

const sentence = 'she\'s my "kind" of girl';

const hello = `Hello My name is ${name} and I'm ${38} years old`;

const smarties = 20;
const kids = 3;

const eachKidsGets = Math.floor(smarties / kids);

const person = {
  name: 'benjamin',
  middle: 'Marigi',
  last: 'Ndegwa',
};

console.log(`Each kid will get ${person.last}`);
